server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /epk {
        try_files /epk.html =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
